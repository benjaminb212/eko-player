import { Views, Votes } from '../components';
export default function Social(props) {
    return (
        <div className="social">
            <Views />
            <Votes />
        </div>
    );
}
